describe('<App />', () => {
  beforeEach(async () => {
    await device.reloadReactNative();
  });

  // it('should remove the todo if the delete button of the todo is pressed', async () => {

  // });

  // it('if the first ToDo item has been removed from the app, the second item should now become the first (and only) item', async () => {

  // });
});
