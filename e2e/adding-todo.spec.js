describe('<App />', () => {
  beforeEach(async () => {
    await device.reloadReactNative();
  });

  // it('should open a modal when the add button is pressed', async () => {

  // });

  // it('should render an input field for typing new ToDo items', async () => {

  // });

  // it('should prevent the user create empty todo', async () => {

  // });

  // it('should show an alert if the input is empty and user presses add todo', async () => {

  // });

  // it('should add new todo when add button is pressed and content of input is not empty', async () => {

  // });
});
