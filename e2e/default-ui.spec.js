describe('<App />', () => {
  beforeEach(async () => {
    await device.reloadReactNative();
  });

  // it('should render two default ToDo items', async () => {

  // });

  // it('should render an add button for adding todo items', async () => {

  // });

  // it('each todo item should render a DELETE button', () => {

  // });
});
